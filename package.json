{
  "name": "rusthacks",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:validate": "astro build && node scripts/validate-sitemaps.mjs",
    "optimize:images": "node scripts/optimize-responsive-images.mjs",
    "fetch:images": "node scripts/generate-hack-ui-images.mjs",
    "preview": "astro preview",
    "validate:sitemaps": "node scripts/validate-sitemaps.mjs",
    "generate:i18n": "node scripts/generate-i18n-content.mjs",
    "deploy": "npm run build && wrangler deploy",
    "pages:deploy": "wrangler pages deploy dist --project-name=rusthacks",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/outfit": "^5.2.8",
    "@fontsource-variable/syne": "^5.2.7",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.9",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "terser": "^5.49.0",
    "wrangler": "^4.30.0"
  }
}
